# Get-links-dropbox

Copy + paste vào Terminal
1. Cài đặt module cần thiết (): 
    pip install dropbox pandas openpyxl tqdm

2. Run:
    python get_links.py

Kết quả nằm trong file .xlsx => download.